// Collecting class information and storing it in variables
let subjectTitle = "DSA";
let classSchedule = "8:00 AM - 9:30 AM, Tue-Thur";
let classroom = "Room 121";
let classInstructor = "Mr. VJ";
let studentName = "Bhlezz Garbin";

// Logging the information in the specified format
console.log(`${studentName} is currently enrolled in ${subjectTitle} with a class schedule of ${classSchedule} at ${classroom}. The instructor for the subject is ${classInstructor}.`);
